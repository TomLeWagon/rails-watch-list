<%# <div class="position-relative overflow-hidden p-3 p-md-5 m-md-3 text-center bg-light"> %>
  <div class="home-banner" style="background-image: linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.3)), url('https://source.unsplash.com/random');">
    <h1 class="display-1"> Tom's Movie List </h1>
  </div>
<%# </div> %>
<%# <%= cl_image_tag("lgrhswubbcpjns4prerg", width: 400, height: 300, crop: :fill) %>
<div class="container">
<%= link_to "New List", new_list_path, class: "btn btn-primary"%>
  <ul class="list-group">
    <% @lists.each do |list| %>
      <li class="list-group-item" >
        <h3><%=  list.name %></h3>
      <%= link_to "See details", list_path(list), class: "btn btn-primary"%>
      </li>
    <% end %>
  </ul>
</div>
