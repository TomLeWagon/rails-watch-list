 <div class="container">
  <div class="bannershow" style="background-image: linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.3)), url(<%= cl_image_path @list.photo.key %>)">
    <h3><%= @list.name %></h3>

  </div>

  <%= link_to 'Add Movie', new_list_bookmark_path(@list), class: "btn btn-primary"%>
  <%= link_to 'Back', lists_path, class: "btn btn-primary" %>
  <%= link_to "🔥 Delete",list_path(@list),
    class: "btn btn-primary",
    method: :delete,
    data: { confirm: "Are you sure?" }
  %>

  <ul class="list-group">
    <% @list.bookmarks.each do |bookmark| %>
      <li class="list-group-item"><%= bookmark.movie_id %> <%= bookmark.comment %>
      </li>
    <% end %>
  </ul>

</div>
